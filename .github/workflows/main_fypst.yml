# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure App Service: https://aka.ms/python-webapps-actions

name: Build and deploy Python app to Azure Web App - fypst

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python version
      uses: actions/setup-python@v1
      with:
        python-version: '3.8'

    - name: Create and start virtual environment
      run: |
        python -m venv venv
        source venv/bin/activate
    
    - name: Install dependencies
      run: pip install -r requirements.txt
      
    # Optional: Add step to run tests here (PyTest, Django test suites, etc.)
    
    - name: Upload artifact for deployment jobs
      uses: actions/upload-artifact@v2
      with:
        name: python-app
        path: |
          . 
          !venv/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    
       
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v2
        with:
          name: python-app
          path: .
      
      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'fypst'
          slot-name: 'production'
          credentials: '<publishData><publishProfile profileName="fypst - Web Deploy" publishMethod="MSDeploy" publishUrl="fypst.scm.azurewebsites.net:443" msdeploySite="fypst" userName="$fypst" userPWD="ouo49fB3blDM7yeNgaredzpLz9bpgtYlnvh6nKyQG6NiRahQe8JhG255bwpM" destinationAppUrl="http://fypst.azurewebsites.net" SQLServerDBConnectionString="" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="http://windows.azure.com" webSystem="WebSites"><databases /></publishProfile><publishProfile profileName="fypst - FTP" publishMethod="FTP" publishUrl="ftp://waws-prod-hk1-031.ftp.azurewebsites.windows.net/site/wwwroot" ftpPassiveMode="True" userName="fypst\$fypst" userPWD="ouo49fB3blDM7yeNgaredzpLz9bpgtYlnvh6nKyQG6NiRahQe8JhG255bwpM" destinationAppUrl="http://fypst.azurewebsites.net" SQLServerDBConnectionString="" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="http://windows.azure.com" webSystem="WebSites"><databases /></publishProfile><publishProfile profileName="fypst - Zip Deploy" publishMethod="ZipDeploy" publishUrl="fypst.scm.azurewebsites.net:443" userName="$fypst" userPWD="ouo49fB3blDM7yeNgaredzpLz9bpgtYlnvh6nKyQG6NiRahQe8JhG255bwpM" destinationAppUrl="http://fypst.azurewebsites.net" SQLServerDBConnectionString="" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="http://windows.azure.com" webSystem="WebSites"><databases /></publishProfile><publishProfile profileName="fypst - ReadOnly - FTP" publishMethod="FTP" publishUrl="ftp://waws-prod-hk1-031dr.ftp.azurewebsites.windows.net/site/wwwroot" ftpPassiveMode="True" userName="fypst\$fypst" userPWD="ouo49fB3blDM7yeNgaredzpLz9bpgtYlnvh6nKyQG6NiRahQe8JhG255bwpM" destinationAppUrl="http://fypst.azurewebsites.net" SQLServerDBConnectionString="" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="http://windows.azure.com" webSystem="WebSites"><databases /></publishProfile></publishData>'
          publish-profile: ${{ secrets.AzureAppService_PublishProfile_c7911cc64a73443dadbab2c30456b86b }}